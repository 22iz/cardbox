        # # 需要通过链接下载图片
        # photo_url = "http://120.24.224.48:8000/img/qbnogu_214333134.jpeg"
        # extensin ='.'+photo_url.split('.')[-1]
        # photo_rename = fname + '_' + str(cardboxer_id) + extension
        # photo_save_url = "/Users/isaaczhang/Documents/tornado_projects/firstTry/img/" + photo_rename
        # # photo_save_url = "/home/feilong/tornado_projects/firstTry/img/" + fname + '_' + str(cardboxer_id) + extension
        # urllib.urlretrieve(url, photo_save_url)

        # photo = self.get_argument('file')
        # final_filename= fname+"_"+str(cardboxer_id)+'.jpg'
        # output_file = open("img/" + final_filename, 'w')
        # output_file.write(photo)
        # self.write("file" + final_filename + " is uploaded<br/><br/>")